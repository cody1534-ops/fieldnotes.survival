# Field Notes — Start Screen Edition

Upload every file in this folder to the root of your GitHub repository, then enable GitHub Pages from `main` and `/(root)`. The game first opens on a start screen. **Continue your run** loads the local saved game. **Start a new run** resets the current run while retaining achievements and best day.
